import { EmptyState } from "@/components/EmptyState";

export default function Loading() {
  return <EmptyState title="Loading HomeStock" description="Pulling together the household grocery memory." />;
}
